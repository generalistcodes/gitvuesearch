import 'jest';
import { shallowMount } from '@vue/test-utils';
import GitSearch from '@/components/GitSearch.vue';  

describe('GitSearch', () => {
    test('checking...', () => {
        
        console.log('GitSearch:', GitSearch)
    });
});